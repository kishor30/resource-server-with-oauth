**Cognito resource server with client credential Oauth flow**

Basic POC app to connect to cognito for authentication and authorization with spring security configuration

**Steps to run the app**

- mvn clean

- mvn spring-boot:run 